如果决赛真的把题目从“简单数组遍历”升级到更明显的“算法题”，我认为也大概率不会变成 ACM 那种图论、复杂 DP，而会是**能用几十行 C、再比较容易翻译成 LA32R 汇编的基础算法**。

原因很明确：你给的 2025 个人赛总结直接说，2025 编程题和上一年“本质一样就是遍历一遍数组”，2025 题目本身是“统计数组中第一个元素出现的次数”，而且性能重点仍然是循环展开和连续访存。 因此下面我把“已有资料能支持的趋势”和“我对可能升级方向的推测”分开说。

## 最可能：数组上的 O(n) 算法

这是我认为需要重点准备的。

题目可能长这样：

```text
给定 N 个 32 位整数：
求最大值 / 最小值
求第二大值
统计某类元素数量
寻找第一个满足条件的元素
求和 / XOR / 校验值
原地修改数组
数组反转
两数组逐元素运算
```

例如“第二大值”已经比单纯最大值更像算法题：

```c
uint32_t max1 = 0;
uint32_t max2 = 0;

for (...) {
    uint32_t x = *p++;

    if (x > max1) {
        max2 = max1;
        max1 = x;
    }
    else if (x > max2) {
        max2 = x;
    }
}
```

汇编上仍然只是：

```text
ld.w
bltu / bgeu
or / add.w
addi.w
bne
```

非常符合决赛特点。

---

## 很可能：数组双指针

这是我觉得**非常值得准备**的一类，因为它比去年难一点，但不会难到不适合现场写汇编。

典型题目：

```text
数组反转
首尾交换
删除某类元素
把符合条件的元素搬到前面
奇数放左边、偶数放右边
两端向中间搜索
```

例如奇偶划分：

```c
while (l < r) {

    while (l < r && ((*l & 1) == 0))
        l++;

    while (l < r && ((*r & 1) != 0))
        r--;

    if (l < r) {
        uint32_t t = *l;
        *l = *r;
        *r = t;
    }
}
```

这里考的是：

* 指针
* 比较
* 分支
* `AND`
* load/store

并没有引入复杂 ISA。

---

## 很可能：位运算算法

这个尤其符合 LoongArch 汇编题。

例如：

```text
统计 1 的个数
判断 2 的幂
提取某些 bit
bit reverse
循环移位
高低 16 位交换
字节序转换
mask 运算
CRC / checksum
```

龙架构基础整数指令本身就专门包含移位和位操作类指令；参考手册还单独列出了 CRC 校验指令。

例如经典 popcount：

```c
uint32_t count = 0;

while (x != 0) {
    x &= x - 1;
    count++;
}
```

这是一个非常适合出汇编题的小算法：

```text
x = 10110100
x-1
x &= x-1
```

每循环一次消掉最低位的一个 `1`。

我会把这类放在**高优先级**。

---

## 很可能：整数数学算法

难度可能比数组扫描高一档，但仍然非常适合汇编。

我建议至少准备：

```text
GCD
整数平方根
整数除法
幂
绝对值
最大公约数
取模思想
```

### 欧几里得 GCD

如果支持 `%`：

```c
while (b != 0) {
    uint32_t t = a % b;
    a = b;
    b = t;
}
```

如果不想依赖除法/取模，也可能设计成减法版：

```c
while (a != b) {
    if (a > b)
        a -= b;
    else
        b -= a;
}
```

这个版本尤其适合简单 CPU。

---

### 整数平方根

你已经练过的：

```c
while (l <= r) {
    uint32_t mid = l + ((r - l) >> 1);

    if (mid * mid <= x) {
        ans = mid;
        l = mid + 1;
    } else {
        r = mid - 1;
    }
}
```

这里综合考察：

```text
二分
乘法
无符号比较
移位
```

非常像决赛可能出现的“小算法”。

---

## 中等概率：二分查找

例如：

> 给定一个已经递增排序的数组，寻找第一个 ≥ key 的元素。

```c
uint32_t l = 0;
uint32_t r = n;

while (l < r) {

    uint32_t mid = l + ((r - l) >> 1);

    if (a[mid] < key)
        l = mid + 1;
    else
        r = mid;
}
```

这是我建议你一定会写的。

因为它只有：

```text
加减
右移
load
compare
branch
```

而且可以很好地测试考生是否真的会写汇编。

---

## 中等概率：简单排序

如果真的考“传统算法”，我认为排序里最可能的是：

### 冒泡排序

```c
for (i = 0; i < n; i++) {
    for (j = 0; j + 1 < n - i; j++) {

        if (a[j] > a[j + 1]) {
            uint32_t t = a[j];
            a[j] = a[j + 1];
            a[j + 1] = t;
        }
    }
}
```

或者：

### 选择排序

```c
for (i = 0; i < n; i++) {

    uint32_t min = i;

    for (j = i + 1; j < n; j++)
        if (a[j] < a[min])
            min = j;

    swap(a[i], a[min]);
}
```

我反而认为**选择排序比冒泡更值得准备**。

因为汇编层：

* 访存次数容易控制；
* 算法结构简单；
* 没有递归；
* 不需要栈。

---

## 中低概率：前缀和

例如：

```text
输入：
1 2 3 4 5

输出：
1 3 6 10 15
```

C：

```c
uint32_t sum = 0;

for (...) {
    sum += *src++;
    *dst++ = sum;
}
```

本质其实还是：

> READ → 运算 → WRITE

所以这种题既可以软件做，也特别容易映射成你现在准备的硬件加速器。

---

## 中低概率：滑动窗口

例如：

> 求连续 K 个元素的最大和。

最笨：

```c
for each i:
    sum A[i ... i+K-1]
```

优化后：

```c
sum = first K;

for (i = K; i < n; i++) {
    sum += a[i];
    sum -= a[i-K];

    if (sum > ans)
        ans = sum;
}
```

这种已经有一点“算法竞赛”的味道，但仍然很适合汇编。

如果今年出题人希望**明显提高算法含量但仍保持汇编可写性**，我认为滑动窗口就是一个很合理的选择。

---

## 中低概率：字符串

因为本质上只是把：

```text
uint32_t
```

换成：

```text
uint8_t
```

题目可能是：

```text
统计字符
字符串反转
找最长连续相同字符
大小写转换
数字字符串转整数
回文判断
```

例如回文：

```c
while (l < r) {

    if (*l != *r)
        return 0;

    l++;
    r--;
}

return 1;
```

LA32R 中 load/store 架构非常适合这种题，字节访存用 `ld.b/ld.bu/st.b`。龙架构本身也是标准 load/store RISC 架构，普通计算都在寄存器中进行。

---

# 我认为“很不可能”的算法

现阶段我不会花太多时间准备：

```text
DFS / BFS
Dijkstra
并查集
线段树
树状数组
复杂动态规划
KMP
快速排序递归版
归并排序
红黑树
链表复杂操作
堆
哈希表
```

不是说绝对不可能，而是从现有比赛资料来看，**没有证据表明个人赛决赛正在往这种方向走**。

而且这些算法有几个问题：

```text
需要大量栈
大量随机访存
代码很长
汇编 debug 困难
现场时间成本高
```

和过去“数组 + ExtRAM + 运行时间计分”的风格并不吻合。

---

# 如果让我预测一道“升级版 2026 题”

我反而会重点防这种：

> 给定 `0x80400000 ~ 0x806fffff` 中的 32 位无符号整数数组，求**最长连续递增子段长度**，将结果写入 `0x80700000`。

C 只需要：

```c
uint32_t prev = *p++;
uint32_t cur = 1;
uint32_t best = 1;

while (p != end) {

    uint32_t x = *p++;

    if (x > prev) {
        cur++;

        if (cur > best)
            best = cur;
    }
    else {
        cur = 1;
    }

    prev = x;
}
```

但是它比：

```text
统计第一个元素出现次数
```

明显多了一层算法性。

同时它仍然满足：

```text
单遍扫描 O(n)
连续 ExtRAM
只需几个寄存器
没有复杂数据结构
很好翻译成汇编
也很好做硬件加速器
```

**这种题我认为是非常值得防的。**

---

## 所以你现在最应该准备的算法顺序

我会按：

**第一梯队：**

```text
最大/最小/第二大
计数
最长连续段
数组反转
双指针
sum / xor / checksum
位运算
```

**第二梯队：**

```text
二分查找
GCD
整数 sqrt
无除法除法
前缀和
滑动窗口
回文/字符串
```

**第三梯队：**

```text
选择排序
冒泡排序
简单矩阵操作
```

最后才考虑：

```text
复杂排序
DP
图论
树
```

尤其建议你把**“最长连续段、第二大值、双指针、滑动窗口”**补上。它们正好处在一个很危险的位置：**比往年题难一点，但又完全没有难到不适合龙芯杯汇编决赛。**

如果今年出题人觉得前两年“遍历数组”太简单，需要提升区分度，我认为这几类比突然上 DFS/DP 合理得多。
